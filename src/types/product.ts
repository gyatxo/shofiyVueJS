interface Product {
    id: number,
    name: string,
    createDate: number,
    price: string,
    image: string,
    stock: number,
    category: Array,
    productCount: number
}

export default Product;